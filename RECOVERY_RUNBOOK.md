# Jerboa Circle Recovery Runbook

대상: 1인 Keeper 운영
원칙: 문제가 생기면 먼저 공개를 멈추고, 복구 파일을 받은 뒤, 가장 작은 범위만 되돌린다.

## 1. 잘못된 내용이 공개됨

1. 해당 프로그램을 열고 `게시 취소`를 누른다.
2. 공개 목록, 상세 주소, 사이트맵, 피드에서 사라졌는지 확인한다.
3. `Revision history` 또는 `Published editions`에서 올바른 판본으로 복구 초안을 만든다.
4. 비공개 미리보기로 확인한 뒤 새 판본을 게시한다.

게시 취소는 이력을 삭제하지 않는다. 기존 발행 지문을 수정하지 말고 새 발행본으로 남긴다.

## 2. 기록을 실수로 삭제함

1. Keeper의 `삭제 보관함`을 연다.
2. 제목과 ID를 확인하고 해당 프로그램을 복원한다.
3. 복원 결과는 우선 비공개 상태에서 확인한다.
4. 관계 검사에서 끊어진 프로그램·자료 연결이 없는지 확인한다.
5. 필요할 때만 다시 게시한다.

자료를 삭제하면 Programme의 `referenceIds`에서도 제거된다. 복원 후 필요한 관계는 직접 다시 선택한다.

## 3. 다른 창의 변경과 충돌함

1. 저장을 반복해서 누르지 않는다.
2. `내 변경사항을 복구 파일로 보존`을 먼저 실행한다.
3. Programme, Collection, 자료 장부, 공개 문구별로 서버판 또는 내 임시 저장을 고른다.
4. 차이 목록을 확인하고 병합한다.
5. 병합 직후 공개 상세와 운영 기록을 확인한다.

서버판과 로컬판을 자동으로 섞지 않는다. 발행 이력은 양쪽을 합쳐 보존한다.

## 4. 공동 장부 저장이 실패함

1. 화면의 `이 기기에 저장됨` 시각을 확인한다.
2. 연결이 돌아올 때까지 탭을 닫지 않는다.
3. 닫아야 한다면 `백업과 관리 -> 파일 백업` 또는 충돌 복구 파일을 받는다.
4. 온라인 복귀 안내 후 `변경사항 저장`을 다시 누른다.
5. 서버가 더 최신이면 충돌 선택 화면으로 이동한다.

오류 중에도 로컬 자동 복구본은 별도 목록에 남는다. 서버에 한 번도 저장되지 않은 새 프로그램도 제목과 시각으로 불러올 수 있다.

## 5. 오래된 장부를 덮어썼거나 JSON이 손상됨

1. `운영 상태와 백업`에서 최신 자동 백업 날짜와 크기를 확인한다.
2. 복원 전에 현재 장부를 파일로 내려받는다.
3. 백업 또는 import 파일을 적용하면 먼저 구조 검사와 변경 요약을 확인한다.
4. corrupted JSON, schema 불일치, 없는 관계 ID는 적용하지 않는다.
5. 검증된 백업만 복원하고 공개 기록을 표본 확인한다.

자동 백업은 날짜별로 보존되며 복원 작업도 audit log에 남는다.

## 6. 이미지가 보이지 않음

1. record의 이미지 URL과 alt를 확인한다.
2. 공동 저장소 URL을 새 query로 직접 열어 상태를 확인한다.
3. `404`면 원본 파일을 다시 올리고 기록을 저장한다.
4. record가 저장되지 않은 미공유 이미지는 자동 cleanup 대상이다.
5. 이미 조회한 immutable URL의 오래된 edge cache를 Blob 존재 증거로 사용하지 않는다.

같은 바이트는 digest URL을 재사용한다. 현재 공동 장부에서 참조 중인 이미지는 DELETE 요청을 거부한다.

## 7. 예약 공개가 예상과 다름

1. KST 기준 시작·종료 시각과 순서를 확인한다.
2. visibility가 `public`, workflow가 `published`인지 확인한다.
3. 시작 전 또는 종료 후 상세 `404`는 정상이다.
4. `archive.json`, 상세, sitemap, feed를 함께 확인한다.
5. invalid 또는 역전 schedule은 fail closed로 숨겨진다.

## 사고 기록에 남길 것

- 발생 시각과 발견자
- record ID와 제목
- 마지막 정상 revision
- 수행한 action: publish, unpublish, delete, restore, sync
- 복구에 사용한 backup 날짜 또는 publication hash
- 공개 표면의 최종 HTTP 상태

본문 전체, pass key, session token, private participant 정보는 audit note에 복사하지 않는다.
