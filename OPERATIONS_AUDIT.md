# Jerboa Circle 운영 안정화 감사 기록

기준일: 2026-07-31

## 1. 현재 구조

- 공개 영역: 홈·Archive(`/`, `/archive/`), 기록 상세(`/archive/:id/`), Catalogue(`/catalogue/`, `/catalogue/:id/`).
- 참여자 영역: Reader folios·Itinerary·Cabinet·Folio(`/members/`).
- 운영 영역: Keeper Desk(`/keeper/`)와 문구실(`/godmode/`).
- 영속성: Vercel Blob 기반 동기화 API와 이미지 저장소를 사용하며, 브라우저 로컬 저장소는 초안·복구·읽기 표시를 위한 보조 계층이다.
- 공개 판정: `visibility`, `workflowStatus`, `publishAt`, `unpublishAt`를 함께 적용한다.
- 빌드: Vite 정적 자산 뒤에 공개 라우트별 HTML 메타데이터, sitemap, feed, archive JSON과 Sites 출력물을 생성한다.

## 2. 진단과 우선순위

| 심각도 | 발견 사항 | 조치 |
| --- | --- | --- |
| Critical | 번들에 포함된 공개 기록을 비공개로 전환해도 공개 동기화 응답에서 override가 사라져 원본이 다시 노출될 수 있음 | 민감 필드를 제외한 비공개 tombstone을 공개 snapshot에 남겨 원본을 확실히 가리도록 수정 |
| High | 임시 저장·공동 저장·게시·게시 취소의 의미가 작업 막대에서 충분히 분리되지 않음 | 버튼과 상태 문구를 분리하고 게시 취소를 독립 작업으로 구현 |
| High | 미완성 초안도 전체 게시 검증에 걸려 로컬 임시 저장이 막힐 수 있음 | 로컬 초안은 불완전 상태로 저장 가능, 공개·발행 기록의 공동 저장과 게시만 전체 검증 |
| High | 상세 화면과 Keeper에 편집 흐름이 중복되어 상태 출처가 갈릴 수 있음 | 상세 화면의 인라인 편집 진입을 제거하고 해당 기록이 선택된 Keeper 단일 흐름으로 연결 |
| High | Archive에 유형·주제·정렬 조합이 없고 공유 가능한 상태가 제한됨 | 검색·상태·시즌·컬렉션·유형·주제·정렬을 URL에 보존 |
| High | Cabinet 관계가 공통 태그만으로 자동 추론되어 편집자의 의도와 달라질 수 있음 | `relatedEntryIds` 직접 관계를 추가하고 선택·상호 연결만 노출 |
| Medium | Cabinet의 검색·서랍·facet·정렬 상태와 상세 왕복 맥락이 사라짐 | 모든 상태를 URL에 보존하고 목록 스크롤 복원 구현 |
| Medium | 외부/이전 이미지 실패 시 카드와 상세의 의미가 사라짐 | 접근 가능한 이미지 fallback 컴포넌트와 스타일 추가 |
| Medium | 정적 상세의 canonical·Open Graph·Twitter 메타가 불완전함 | 라우트별 canonical, robots, OG/Twitter, 예약 공개 판정을 생성 단계에 추가 |
| Medium | 모바일 Cabinet의 본문형 링크 4개가 안정적인 터치 높이에 못 미침 | 44px 최소 터치 영역 적용 |
| Low | 상세 페이지 안에 더 이상 렌더링하지 않는 예전 인라인 편집 컴포넌트 정의가 남아 있음 | 사용자 경로에서는 비활성화. 후속 코드 정리 대상으로 남김 |

## 3. 실제 변경 범위

### Keeper Desk

- `?record=<id>`로 특정 프로그램을 바로 열 수 있다.
- 디바운스 자동 복구본과 수동 `임시 저장`은 기기 안에 저장된다.
- `변경사항 저장`은 인증된 공동 장부 동기화이고, `변경사항 게시`는 발행 전 검증·발행 이력·공개 반영을 수행한다.
- `게시 취소`는 확인 후 비공개·보관 상태와 취소 시각을 공동 장부에 기록한다.
- 저장 중·성공·실패·충돌·게시/취소 중 상태를 작업 막대에 표시한다.
- 편집 중 이탈 경고와 서버 충돌 복구 경로는 유지했다.

### Archive

- 검색, 공개 상태, 시즌, 컬렉션, 유형, 주제, 최신/오래된 순 정렬을 조합할 수 있다.
- 필터는 URL query로 복원·공유 가능하다.
- 공개 목록과 정적 산출물 모두 예약 시작/종료 시각을 반영한다.
- 기록 상세의 편집은 Keeper Desk 한 곳으로 연결된다.

### Cabinet

- 나의 수장고/공동 열람실, 서랍, 검색, 주제, 형식, 시대, 지역, 정렬을 URL에 저장한다.
- 관계는 공통 태그 자동 추론이 아니라 편집자가 고른 직접 관계만 사용한다.
- 목록에서 상세로 들어갔다 돌아와도 검색·필터·정렬과 스크롤 맥락을 유지한다.

### 접근성·오류·검색 엔진

- 전역 키보드 포커스 표시와 이미지 대체 상태를 강화했다.
- 없는 일반 라우트는 별도 404 문구와 `noindex, nofollow`를 사용한다.
- 보호 라우트도 noindex이며 공개 라우트는 canonical과 소셜 미리보기 이미지를 갖는다.

## 4. 데이터 모델과 마이그레이션

- `Curiosity.relatedEntryIds?: string[]`만 선택 필드로 추가했다. 기존 Cabinet 데이터는 변환 없이 그대로 읽힌다.
- 프로그램 게시 취소는 기존 `unpublishAt`, `visibility`, `workflowStatus`, 감사 로그를 사용한다.
- 공개 snapshot의 비공개 tombstone 형식은 새 비밀 데이터를 포함하지 않는다.
- 데이터베이스 교체, 전체 레코드 재작성, 삭제성 마이그레이션은 없다.

## 5. 유지한 것

- 중세 필사본·고전 도감·흑백 에디토리얼·브루탈리즘·논문형 타이포그래피 정체성.
- 기존 콘텐츠, 핵심 페이지 계층, 참여자 장부, Vercel Blob/API 운영 방식.
- 현재 이미지·서체 자산과 저작권/출처 표기 구조.
- 서버 우선·로컬 복구본 보조라는 기존 동기화 원칙.

## 6. 검증 기준

- 정적 타입 검사, 프로덕션 빌드, 33개 정적 라우트 smoke test, 자산 성능 예산.
- 실브라우저 360, 390, 768, 1024, 1440, 1920px 폭에서 가로 넘침, 깨진 이미지, 주요 조작 영역 확인.
- Archive 필터 조합과 새로고침 복원, Keeper 로컬 초안 저장·복원·원상복구, Cabinet 검색·facet·정렬·상세 왕복 상태를 확인.
- 실제 공동 장부의 게시/게시 취소는 운영 데이터 변경을 피하기 위해 파괴적 실험을 하지 않고, 인증·검증·충돌·저장 경로를 코드와 빌드로 검증한다.

## 7. 다음 운영 과제 5개

1. 남아 있는 비활성 상세 인라인 편집 컴포넌트와 iCloud 중복 파일을 별도 정리한다.
2. 실제 운영 계정으로 staging 기록 하나를 만들어 저장·게시·게시 취소의 종단 간 회귀 테스트를 자동화한다.
3. 이미지 업로드 시 크기·형식·대체 텍스트·출처를 서버에서도 검증한다.
4. 감사 로그와 발행 이력을 월별 export하고 복구 연습을 정례화한다.
5. 실제 사용량을 기반으로 큰 서체와 원본 이미지의 캐시·서브셋·파생 이미지 정책을 다듬는다.
